// 🚀 PASTE YOUR TEMPORARY LOCALTUNNEL LINK HERE (Ensure there is NO trailing slash)
export const TUNNEL_URL = "https://thick-badgers-float.loca.lt"; 

export const BASE_URL = TUNNEL_URL;
export const API_URL = `${BASE_URL}/api/v1`;

console.log(`[Zero-Space Data Bridge Active]: Intersecting data down via ${API_URL}`);
